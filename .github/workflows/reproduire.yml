name: Needs Reproduction

on:
  issues:
    types: [labeled]

permissions:
  issues: write

jobs:
  reproduire:
    runs-on: ubuntu-latest
    if: github.event.label.name == 'needs reproduction'
    steps:
      - uses: actions/checkout@v4
      - uses: Hebilicious/reproduire@v0.0.9
        with:
          label: needs reproduction
